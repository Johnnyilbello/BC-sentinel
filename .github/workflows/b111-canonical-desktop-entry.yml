name: B11-1 Canonical Desktop Entry

on:
  push:
    branches:
      - feature/v011-beta11-b111-canonical-desktop-entry
  workflow_dispatch:

permissions:
  contents: read

jobs:
  acceptance:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Full regression and B11-1 canonical desktop acceptance
        shell: powershell
        run: ./tools/acceptance/TEST-V011-BETA11-B111.ps1 -ConfirmCanonicalDesktopEntry
